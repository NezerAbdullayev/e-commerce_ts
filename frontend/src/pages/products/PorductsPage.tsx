import { Box } from "@mui/material";
import { FC } from "react";

const ProductsPage: FC = () => {
    return (
        <Box>
            <div>products</div>
        </Box>
    );
};

export default ProductsPage;
